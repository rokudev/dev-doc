---
name: BlockQuote
---
import React from "react";
export const BlockQuote = ({ children, className = "", }) => {
    return (<div className={`bg-amber-50 border border-[#f7e3d3] rounded-[4px] p-5 flex items-center text-left ${className}`}>
      <div className={`text-amber-900 text-sm m-0`}>{children}</div>
    </div>);
};


{/* Example Usage */}
<BlockQuote>
  <div>ArrayGrid is not meant to be instantiated directly by app code</div>
</BlockQuote>