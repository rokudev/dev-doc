---
name: ListComponent
---
export const items = ["Go to the Home Screen", "Navigate to Settings", "Check for updates"];

export const ListItems = ({header, items}) => {
    return (
      <div>
        <h2>{header}</h2>

   			<ul>
					{items.map((item, index) => (<li key={index}> {item}</li> ))}            
				</ul>
			</div>
    );
};

<ListItems header="Sample list" items={items} />