---
name: TableComponent
---
export const TableComponent = ({ header }) => {
  const containerStyle = { 
    maxWidth: '800px', 		
    margin: '5px auto',
    padding: '0 5px 5px 5px', 
    border: '1px solid #ccc', 
    borderRadius: '8px', 
    boxShadow: 'rgba(0, 0, 0, 0.35) 0px 5px 15px',
    backgroundColor: 'oklch(87.2% 0.01 258.338)',
    textAlign: 'left'
  };

  const tableStyle = {
    width: '100%',
    borderCollapse: 'collapse',
    //marginTop: '20px',
    backgroundColor: 'white',
    borderRadius: '4px',
    overflow: 'hidden'
  };

  const headerStyle = {
    backgroundColor: '#f5f5f5',
    fontWeight: 'bold',
    padding: '12px',
    border: '1px solid #ddd',
    textAlign: 'left'
  };
  const cellStyle = {
    padding: '12px',
    border: '1px solid #ddd',
    verticalAlign: 'top'
  };

  const listStyle = {
    margin: '8px 0',
    paddingLeft: '20px'
  };

  const listItemStyle = {
    marginBottom: '4px'
  };


  return (
    <div style={containerStyle}>
      <h2>{header}</h2>
      
			<table style={tableStyle}>
        <thead>
          <tr>
            <th style={headerStyle}>Role</th>
            <th style={headerStyle}>Permissions</th>
            <th style={headerStyle}>Access-level</th>
          </tr>
        </thead>
        
        <tbody>
          {data.map((row, index) => (<tr key={index}>
              <td style={cellStyle}>
                <strong>{row.role}</strong>
              </td>
              
              <td style={cellStyle}>
								{row.description && (
                  <p style={{ margin: '0 0 8px 0', fontWeight: 'normal' }}>
                    {row.description}
                  </p>
                )}
                
                {row.permissions && row.permissions.length > 0 && (
                  <ul style={listStyle}>
                    {row.permissions.map((permission, permIndex) => (
                      <li key={permIndex} style={listItemStyle}>
                        {permission.category && (
                          <strong>{permission.category}:</strong>
                        )} {permission.text}
                        {permission.subItems && permission.subItems.length > 0 && (
                          <ul style={listStyle}>
                            {permission.subItems.map((subItem, subIndex) => (
                              <li key={subIndex} style={listItemStyle}>
                                {subItem}
                              </li>
                            ))}
                          </ul>
                        )}
                      </li>
                    ))}
                  </ul>
                )}
							</td>
              
							<td style={cellStyle}>
                <strong>{row.accessLevel}</strong>
              </td>
            
					</tr> ))} 
        </tbody>

			</table>
		</div>
  );
};

